# Leave unset: the app calls the API same-origin (Vite dev proxy in dev, the
# FastAPI static mount in prod), which keeps LAN/Tailscale access working.
# Only set this for split deploys where the API lives on another host.
#VITE_API_BASE_URL=
